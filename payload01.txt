<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>COMP6013 Week 7 · Quantum Crypto Studio</title>
<meta name="theme-color" content="#eef8ff">
<style>
:root{
  --bg:#eef8ff;
  --bg2:#f9fcff;
  --panel:#ffffff;
  --panel-soft:#f5fbff;
  --text:#15304d;
  --muted:#5a7088;
  --line:#d6e7f3;
  --primary:#1ca7a1;
  --secondary:#5b74ff;
  --accent:#ff8f70;
  --violet:#8b6cff;
  --good:#20b86a;
  --warn:#f1a438;
  --bad:#e35677;
  --shadow:0 14px 40px rgba(43,92,145,.12);
}
*{box-sizing:border-box}
html{scroll-behavior:smooth}
body{
  margin:0;
  font-family:Inter,ui-sans-serif,system-ui,-apple-system,Segoe UI,Arial,sans-serif;
  color:var(--text);
  background:
    radial-gradient(circle at 0% 0%, rgba(28,167,161,.12), transparent 28%),
    radial-gradient(circle at 100% 0%, rgba(91,116,255,.10), transparent 24%),
    radial-gradient(circle at 50% 100%, rgba(255,143,112,.10), transparent 22%),
    linear-gradient(180deg,var(--bg),var(--bg2));
}
body:before{
  content:"";position:fixed;inset:0;pointer-events:none;z-index:-1;opacity:.25;
  background-image:linear-gradient(rgba(91,116,255,.08) 1px,transparent 1px),linear-gradient(90deg,rgba(91,116,255,.08) 1px,transparent 1px);
  background-size:32px 32px;animation:gridMove 24s linear infinite;
}
@keyframes gridMove{to{background-position:32px 32px}}
a{color:inherit;text-decoration:none}
button{font:inherit}
#progress{position:fixed;left:0;top:0;height:4px;width:0;background:linear-gradient(90deg,var(--primary),var(--secondary),var(--accent));z-index:999}
.layout{display:grid;grid-template-columns:280px minmax(0,1fr);min-height:100vh}
.sidebar{
  position:sticky;top:0;height:100vh;padding:18px 16px;border-right:1px solid var(--line);
  background:rgba(255,255,255,.62);backdrop-filter:blur(16px)
}
.logo{padding:12px 14px;border-radius:20px;background:linear-gradient(135deg,#fff,#f0fbfb);border:1px solid var(--line);box-shadow:var(--shadow)}
.logo small{display:block;color:var(--primary);font-weight:900;letter-spacing:.12em;text-transform:uppercase;font-size:.7rem}
.logo h1{margin:6px 0 8px;font-size:1.35rem;line-height:1.05;letter-spacing:-.04em}
.logo p{margin:0;color:var(--muted);font-size:.86rem;line-height:1.5}
.side-links{margin-top:16px;display:grid;gap:8px;max-height:calc(100vh - 230px);overflow:auto;padding-right:4px}
.side-links a{padding:11px 12px;border-radius:14px;border:1px solid transparent;color:var(--muted);font-weight:700;font-size:.92rem;transition:.2s}
.side-links a:hover,.side-links a.active{background:#fff;border-color:var(--line);color:var(--text);transform:translateX(3px)}
.side-note{margin-top:14px;padding:14px;border-radius:18px;background:linear-gradient(135deg,#fff7ef,#fff);border:1px solid #f7ddca;font-size:.86rem;color:#7a5a47;line-height:1.5}
.content{padding:0 28px 60px}
.topbar{position:sticky;top:0;z-index:40;display:flex;gap:12px;align-items:center;justify-content:space-between;padding:14px 0;background:linear-gradient(180deg,rgba(238,248,255,.96),rgba(238,248,255,.78),rgba(238,248,255,0));backdrop-filter:blur(12px)}
.pill{display:inline-flex;align-items:center;gap:8px;padding:8px 12px;border-radius:999px;background:#fff;border:1px solid var(--line);box-shadow:var(--shadow);font-size:.8rem;font-weight:800;color:var(--muted)}
.hero{display:grid;grid-template-columns:1.2fr .85fr;gap:20px;align-items:center;padding:18px 0 34px}
.hero-card,.panel{background:rgba(255,255,255,.86);border:1px solid var(--line);border-radius:28px;box-shadow:var(--shadow)}
.hero-card{padding:30px;position:relative;overflow:hidden}
.hero-card:after{content:"";position:absolute;inset:auto -80px -80px auto;width:220px;height:220px;border-radius:50%;background:radial-gradient(circle,var(--accent),transparent 60%);opacity:.14}
.eyebrow{color:var(--primary);font-size:.75rem;font-weight:900;letter-spacing:.16em;text-transform:uppercase}
.hero h2{font-size:clamp(2.8rem,6vw,5.6rem);line-height:.9;letter-spacing:-.07em;margin:.2em 0}
.hero p{font-size:1.08rem;line-height:1.75;color:var(--muted);max-width:780px}
.hero-actions{display:flex;gap:10px;flex-wrap:wrap;margin-top:18px}
.btn{padding:12px 15px;border-radius:14px;border:1px solid var(--line);background:#fff;font-weight:800;color:var(--text);cursor:pointer;box-shadow:var(--shadow)}
.btn.primary{background:linear-gradient(135deg,var(--primary),var(--secondary));color:#fff;border:none}
.btn:hover{transform:translateY(-2px)}
.visual-lab{padding:24px;min-height:360px;position:relative;overflow:hidden}
.orb{position:absolute;border-radius:50%;border:2px dashed rgba(91,116,255,.26);animation:spin 22s linear infinite}
.orb.one{width:250px;height:250px;left:50%;top:50%;transform:translate(-50%,-50%)}
.orb.two{width:310px;height:170px;left:50%;top:50%;transform:translate(-50%,-50%) rotate(35deg);border-color:rgba(28,167,161,.30);animation-duration:17s}
.orb.three{width:180px;height:330px;left:50%;top:50%;transform:translate(-50%,-50%) rotate(-22deg);border-color:rgba(255,143,112,.30);animation-duration:14s}
@keyframes spin{to{transform:translate(-50%,-50%) rotate(360deg)}}
.core{position:absolute;left:50%;top:50%;transform:translate(-50%,-50%);width:88px;height:88px;border-radius:50%;background:radial-gradient(circle at 35% 35%,#fff 0 18%,#c2fff3 19% 35%,#8fdcff 36% 56%,#7aa1ff 57% 75%,rgba(255,255,255,0) 76%);box-shadow:0 0 40px rgba(91,116,255,.25);animation:pulse 2.4s ease-in-out infinite}
@keyframes pulse{50%{transform:translate(-50%,-50%) scale(1.08)}}
.floating{position:absolute;padding:8px 10px;border-radius:14px;background:#fff;border:1px solid var(--line);font-size:.78rem;font-weight:800;box-shadow:var(--shadow);animation:floatY 4s ease-in-out infinite}
.f1{left:10%;top:16%}.f2{right:12%;top:20%;animation-delay:-1s}.f3{left:15%;bottom:16%;animation-delay:-2.2s}.f4{right:10%;bottom:18%;animation-delay:-3s}
@keyframes floatY{50%{transform:translateY(-10px)}}
.hero-map{display:grid;grid-template-columns:repeat(2,1fr);gap:10px;margin-top:18px}
.hero-map div{padding:11px 12px;border-radius:16px;background:var(--panel-soft);border:1px solid var(--line);font-weight:700;color:var(--muted);font-size:.9rem}
section{padding:18px 0 30px;scroll-margin-top:20px}
.section-head{display:grid;grid-template-columns:.8fr 1.2fr;gap:20px;align-items:end;margin-bottom:16px}
.section-head h3{font-size:clamp(2rem,4vw,3.6rem);line-height:.94;letter-spacing:-.05em;margin:.18em 0}
.section-head p{margin:0;color:var(--muted);line-height:1.75;font-size:1rem}
.kicker{color:var(--secondary);font-size:.75rem;font-weight:900;letter-spacing:.16em;text-transform:uppercase}
.grid2{display:grid;grid-template-columns:1fr 1fr;gap:16px}
.grid3{display:grid;grid-template-columns:repeat(3,1fr);gap:16px}
.card{background:var(--panel);border:1px solid var(--line);border-radius:22px;padding:20px;box-shadow:var(--shadow)}
.card h4{margin:0 0 10px;font-size:1.12rem}
.card p,.card li{color:var(--muted);line-height:1.68}
.card strong{colo